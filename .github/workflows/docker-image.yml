name: CI 
on:
  push:
    tags:
      - '*'
  workflow_dispatch:
  
jobs:
  Build:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v3
    - name: install 
      run:  docker-compose down

  
